#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../noflo-nodejs/bin/noflo-nodejs" "$@"
  ret=$?
else 
  node  "$basedir/../noflo-nodejs/bin/noflo-nodejs" "$@"
  ret=$?
fi
exit $ret
