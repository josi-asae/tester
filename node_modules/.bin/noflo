#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../noflo/bin/noflo" "$@"
  ret=$?
else 
  node  "$basedir/../noflo/bin/noflo" "$@"
  ret=$?
fi
exit $ret
